#include <stdio.h>
#include <ctype.h>
int main() {
    char ch;
    scanf("%c",&ch);
    if ((toupper(ch)=='A')||(toupper(ch)=='A')||(toupper(ch)=='A')||(toupper(ch)=='A')||(toupper(ch)=='A'||)){
        printf("Vowel");
    }
    else {
        printf("Consonant");
    }
    return 0;
}